services:
  - type: web
    name: soccer-live
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: SPORTSRC_API_KEY
        sync: false        # You will enter this manually in Render dashboard
      - key: NODE_ENV
        value: production
    autoDeploy: true
